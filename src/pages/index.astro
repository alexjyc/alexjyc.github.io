---
import Layout from '../layouts/layout.astro';
import Entry from '../components/entry.astro';
import { getEntry } from 'astro:content';

const entry = await getEntry('profile', 'index')!;
---

<Layout title="Alex Jeon">
  <Entry entry={entry} />
</Layout>